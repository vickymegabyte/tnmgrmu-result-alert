name: TNMGRMU Result Monitor

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: pip install requests
      - name: Run monitor
        run: python monitor.py
        - name: Run monitor
  env:
    TNMGRMU_BOT_TOKEN: ${{ secrets.TNMGRMU_BOT_TOKEN }}
    TNMGRMU_CHAT_ID: ${{ secrets.TNMGRMU_CHAT_ID }}
  run: python monitor.py

